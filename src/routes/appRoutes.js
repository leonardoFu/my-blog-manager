import React from 'react';
import { Switch } from 'react-router-dom';
import ArticleRoute from 'components/Article/route';
const appRoutes = (
  <Switch>
    {ArticleRoute}
  </Switch>
)
export default appRoutes;
